import type { SelectionRange } from '@testing-library/user-event/dist/types/event/selection';
export declare function modifySelectionPerMouseMove(selectionRange: Range | SelectionRange, { document, target, node, offset, }: {
    document: Document;
    target: Element;
    node?: Node;
    offset?: number;
}): void;
